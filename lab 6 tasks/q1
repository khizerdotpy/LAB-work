#include <stdio.h>

int main() {
    int num,sum=0;
    printf("enter number(enter 0 to stop): ");
    scanf("%d",&num);
    while(num!=0){
        sum+=num;
        printf("enter number(enter 0 to stop): ");
        scanf("%d",&num);
    }
    printf("the sum is %d",sum);
    return 0;
}
