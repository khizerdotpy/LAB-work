 #include <stdio.h>

int main() {
    int num,sum=0;
    printf("enter number: ");
    scanf("%d",&num);
    for(int i=0;num=0;i++){
    
        sum+=num;
        printf("sum is %d",sum);
    }
    
    return 0;
}
