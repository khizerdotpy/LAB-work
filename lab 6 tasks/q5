#include <stdio.h>

int main() 
{
    int i=1;
    for(i=1;i<=6;i++){
        if (i==1 || i==6){
            printf("* * * * *\n");
        } else {
            printf("*       *\n");
        }
    }
    return 0;
}
