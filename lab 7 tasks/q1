#include <stdio.h>

int main() {
    int temp, i, x;
    int arr[6] = {1, 2, 3, 4, 5, 6};
    
temp=arr[5];
    for (i = 0; i < 5; i++) {
    
        arr[5 - i] = arr[4 - i];


    }
    arr[0]=temp;
    for (x = 0; x < 6; x++) {
        printf("%d ", arr[x]);
    }
    
    return 0;
}
